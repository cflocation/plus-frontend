var downloadData = '';var deleteusergroup = [];jQuery(document).on('pagehide', function(event, ui) {    var page = jQuery(event.target);});$(document).bind("mobileinit", function() {    $.mobile.defaultDialogTransition = 'none';});function getUrlVars(name) {    name = name.replace(/[\[]/, "\\[").replace(/[\]]/, "\\]");    var regex = new RegExp("[\\?&]" + name + "=([^&#]*)"),        results = regex.exec(location.search);    return results == null ? "" : decodeURIComponent(results[1].replace(/\+/g, " "));    }function s4() {    return Math.floor((1 + Math.random()) * 0x10000)        .toString(16)        .substring(1);}function guid() {    return s4() + s4() + '-' + s4() + '-' + s4() + '-' + s4() + '-' + s4() + s4() + s4();}$(document).ready(function(){	var 	date 		= new Date();	var 	y 			= date.getFullYear();	var   	m 			= date.getMonth();	var 	firstDay 	= new Date(y, m, 1);	var 	lastDay 	= new Date(y, m + 1, 0);				$("#all_users_report" ).on( "pagecreate", function( event ) {	//	reportofUserAccounts();	} );		$("#new_users_report").on( "pagecreate", function( event) {		var datefrom = $('#nu_datefrom').val();		var dateto   = $('#nu_dateto').val();				if(isNumeric(String(datefrom).substr(0,2))){			reportNewUsers();		}		else{			$('#nu_datefrom').val(firstDay.getMonth()+1+'/'+firstDay.getDate()+'/'+lastDay.getFullYear());			$('#nu_dateto').val(lastDay.getMonth()+1+'/'+lastDay.getDate()+'/'+lastDay.getFullYear());			reportNewUsers(0,firstDay,lastDay);							}	} );				$("tr.user-details-list").hover(		function() {	$( this ).addClass( "user-details-list-on" );	}, 		function() {	$( this ).removeClass( "user-details-list-on" );}		);	});function isNumeric(n) {  return !isNaN(parseFloat(n)) && isFinite(n);}function isDate(date){	return date instanceof Date && !isNaN(date.valueOf());}