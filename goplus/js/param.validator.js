$('.cal').change(function(event){	id 	= $(this).prop('id');	value = $(this).val();		if(value.indexOf("-") != -1){		var date = value.split("-");	}	else if(value.indexOf("/") != -1){		var date = value.split("/");	}	else{		if(id == 'date-start'){	    $("#date-start").datepicker("setDate", new Date());		}		else{	    $("#date-end").datepicker("setDate", +56);			}		return;	}	if(date.length != 3){		resetDateRange();		return;	}		var m = parseInt(date[0], 10), d = parseInt(date[1], 10),  y = parseInt(date[2], 10);	var Y = new Date().getFullYear();	if (String(y).length == 2){		y = '20'+y;	} else if(String(y).length == 1){		y = '200'+y;	}	if(id == 'date-start' && (m >12 || m < 1 || d > 31 || d < 1 || y < Y || y > Y+3) ){    	 $("#date-start").datepicker("setDate", new Date());	    return;	}	if(id == 'date-end' && (m >12 || m < 1 || d > 31 || d < 1 || y < Y || y > Y+3) ){ 	    $("#date-end").datepicker("setDate", +56);	    return;	}	var dateformatted = new Date(y, m - 1, d);	$("#"+id).datepicker("setDate", dateformatted);		schedulerCountWeeksFromDates();		return;});$('.cal').keypress(function (event) {    var charCode = (event.which) ? event.which : event.keyCode;    	if ( charCode != 46 && charCode > 31 && (charCode < 45 || charCode > 57) ){		event.preventDefault();	}});